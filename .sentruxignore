# Exclusions sentrux — code non-prod, ne doit pas influencer le score archi.

# Sidecar Python RAG et scripts maintenance — hors périmètre architecture TypeScript
services
services/
services/**
**/sovereign-rag/**
.nexus/
.nexus/**
.agent/
.agent/**
.agents/
.agents/**
.claude/
.claude/**
.gemini/
.gemini/**
.github/
.github/**
demo/
demo/**

# Artefacts de build
.next/
.next-mcc/
out/
dist/

# Cache de déploiement Firebase (contient un node_modules complet :
# scanné par erreur, il générait 87 faux "cycles" et des god-files fantômes).
.firebase/

# Tests (mocks, fixtures, benchmarks) — leur couplage est volontairement large
# Note : sentrux `no_god_files` scanne toujours ces fichiers (pas de filtre pour cette règle),
# mais preflight.sh filtre `no_god_files` comme non-bloquant. Les 6 saga.*.test.ts sont donc
# des god-files "tolérés" (tests d'intégration cross-pilier par design).
src/tests/
src/**/*.test.ts
src/**/*.test.tsx
src/**/*.spec.ts
src/**/*.spec.tsx

# Scripts de maintenance — pas du code applicatif
scripts/

# Outils de génération / analyse ponctuelle
scratch/
graphify-out/
cartographie/
analysis/

# Dossiers de travail temporaires
tmp/
