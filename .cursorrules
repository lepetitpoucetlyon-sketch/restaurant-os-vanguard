// 🚀 GRADE X RULES - Indexing Optimization
// This file helps the AI (Antigravity/Cursor) focus on what matters.

{
  "indexing": {
    "exclusions": [
      ".next/",
      ".kilo/",
      "node_modules/",
      "dist/",
      "*.log",
      "out/",
      ".vercel/",
      "public/images/" // Ignore large asset folders if needed
    ]
  },
  "rules": [
    "Always prioritize modular imports from @/modules/",
    "Never import directly across domains without using the Nexus Pulse system",
    "Keep components performance-aware (Brigade Light mode)"
  ]
}
