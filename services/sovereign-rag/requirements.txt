# === Sovereign RAG API — Dependencies ===
# Versions épinglées le 2026-07-03 (audit). Les paquets sans version ne sont
# pas installés dans l'environnement local — épingler au déploiement.

# Core API
fastapi==0.136.1
uvicorn==0.47.0
pydantic==2.13.4
python-dotenv==1.2.2
httpx==0.28.1
python-multipart==0.0.28
aiofiles
numpy==2.4.5
sentence-transformers==5.5.0
rank-bm25==0.2.2
PyMuPDF
scikit-learn==1.8.0
jieba

# Production
gunicorn
tenacity==9.1.4
pyyaml==6.0.3

# Auth — utilisateurs + JWT + hachage mots de passe + 2FA
python-jose[cryptography]==3.5.0
bcrypt==5.0.0
pyotp==2.10.0

# Observabilité & sécurité
prometheus-fastapi-instrumentator==8.0.2
cryptography==49.0.0

# Facturation
stripe==15.3.0

# Testing
pytest==9.1.1
pytest-asyncio==1.4.0
pytest-cov==7.1.0
ragas
datasets==5.0.0
