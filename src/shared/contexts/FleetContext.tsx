/**
 * @deprecated Déplacé vers `@/design/contexts/FleetContext` (design layer)
 * et `app/(admin)/admin/mcc/contexts/FleetContext` (MCC layer).
 * Ce re-export reste pour la rétrocompatibilité — préférer l'import direct.
 */
export { useFleet, FleetProvider } from '@/design/contexts/FleetContext';
