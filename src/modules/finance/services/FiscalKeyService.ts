/**
 * Re-export canonique pour rétro-compatibilité interne au module finance.
 * L'implémentation est hébergée dans src/lib/mcc/fiscal/FiscalKeyService.ts (ADR-015).
 */
export { FiscalKeyService } from '@/lib/fiscal';
