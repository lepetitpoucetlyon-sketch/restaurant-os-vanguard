import { makeHumanAdapter } from '@/verticals/_shared/adapters';

/** @wip vertical-forge — Échéance: 2026-11-01. RH retail = 100 % socle universel (shift + heures sup). */
export const RetailHumanAdapter = makeHumanAdapter();
