# HANDOFF — Rapatriement `src/shared/` · 2026-08-12

> **Branche** : `agent/antigravity-exec`  
> **HEAD** : `e1cc2657c` (étape 8b — hooks nexus-core → kernel/hooks)  
> **Gate** : TSC=0 ✅ · cycles=2 ✅ · shared→modules=0 ✅ · lib→modules=0 ✅ · barrels=0 ✅

---

## 1. État d'avancement — ce qui EST fait

| Commit | Étape | Lot déplacé |
|---|---|---|
| `8b7d4aade` | 1 | `shared/contexts/` — 8 shims supprimés |
| `859cc20f6` | 2 | types/services purs → `lib/` + atoms → `store/` |
| `7284d9056` | 3 | 16 hooks utilitaires → `lib/hooks/` |
| `8d30b491f` | 4 | motion+a11y → `design/utils/` |
| `8cce9d216` | 5a | genome/empire/brands/rbac/ModuleRegistry → `kernel/` |
| `1d11bad1a` | 5b | `plugins/` → `kernel/plugins/` (choix user Option 2) — 26 imports mis à jour |
| `457d235f5` | 6 | `nexus-contract.ts` → `kernel/nexus/contracts/` (110+ refs) |
| `e061cb5b1` | 7 | seeds + connector-manifest + action + atoms |
| `538299a0a` | 8a+8f | `providers/` → `kernel/providers/` + `design/providers/` ; `contexts/settings/defaults.ts` → `kernel/nexus/contracts/settings/` |
| `e1cc2657c` | 8b | hooks nexus-core (13) → `kernel/hooks/` |

**shared/ réduit de 123 → 17 fichiers** (`hooks/` 14 + `schemas/` 3 gelés).

---

## 2. Ce qui RESTE — inventaire exact

### 2.A — `src/shared/hooks/` (14 fichiers)

```
index.ts              ← barrel, 66 importeurs (@/shared/hooks')
useBrandCapabilities.ts
useBrandEditor.ts
useConnector.ts       ← type-only (UseConnectorResult)
useCoreOracle.ts
useDLQQuarantine.ts
useFirestoreBrand.ts
useGeminiAgent.ts
useIntelligence.ts
useLexicon.ts
useManagement.ts      ← 0 importeur externe — candidat suppression
useNexusFleet.ts
useStrategicOracle.ts
useVerticalComponent.ts
```

### 2.B — `src/shared/schemas/` (3 fichiers — **GELÉS par design**)

```
primitives.ts   ← GELÉ (project_schema_migration_strategy)
ui.ts           ← GELÉ (jusqu'à chantier schemas)
index.ts        ← GELÉ
```

**Ne pas toucher** ces 3 fichiers dans cette session.

---

## 3. Plan d'exécution — 4 sous-étapes restantes

> **Règle invariante** : 1 sous-étape = 1 commit · TSC=0 + cycles≤2 **avant** chaque commit · commits en `run_in_background: true` (hook graphify ≈ 2 min) · vérifier `git log --oneline -1` ensuite.

### ⚠️ Pré-check obligatoire avant de commencer

```bash
# Confirmer le HEAD et la baseline gate
git log --oneline -3
./scripts/agent-gate.sh

# Vérifier où @/infrastructure résout (conditionne useBrandEditor)
grep -nE '"@/?infrastructure' tsconfig.json
find src -maxdepth 2 -type d -name infrastructure

# Compter les importeurs actuels du barrel
grep -rn "@/shared/hooks'" src --include='*.ts' --include='*.tsx' | wc -l
# Attendu : 66
```

---

### Sous-étape 8c — Hooks intelligence → `modules/intelligence/ia/hooks/`

**5 fichiers** : `useGeminiAgent`, `useNexusFleet`, `useStrategicOracle`, `useIntelligence`, `useCoreOracle`

**Pourquoi cette destination** : ces hooks importent `@/modules/intelligence` → interdit dans kernel/ (inversion de couche). Ils ne sont PAS dans le barrel `@/shared/hooks` → 0 risque de `shared→modules` via barrel.

#### Actions précises

```bash
# 1. Créer le dossier cible
mkdir -p src/modules/intelligence/ia/hooks

# 2. Déplacer les 5 fichiers (git mv préserve l'historique)
git mv src/shared/hooks/useGeminiAgent.ts      src/modules/intelligence/ia/hooks/useGeminiAgent.ts
git mv src/shared/hooks/useNexusFleet.ts       src/modules/intelligence/ia/hooks/useNexusFleet.ts
git mv src/shared/hooks/useStrategicOracle.ts  src/modules/intelligence/ia/hooks/useStrategicOracle.ts
git mv src/shared/hooks/useIntelligence.ts     src/modules/intelligence/ia/hooks/useIntelligence.ts
git mv src/shared/hooks/useCoreOracle.ts       src/modules/intelligence/ia/hooks/useCoreOracle.ts

# 3. Re-TSC immédiatement (les git mv peuvent casser des imports relatifs internes)
npx tsc --noEmit 2>&1 | grep "error TS" | head -20
# → lire les TS2307, corriger les paths relatifs cassés si nécessaire

# 4. Identifier les importeurs directs (PAS via barrel) et les réécrire
grep -rn "shared/hooks/useGeminiAgent\|shared/hooks/useNexusFleet\|shared/hooks/useStrategicOracle\|shared/hooks/useIntelligence\|shared/hooks/useCoreOracle" src --include='*.ts' --include='*.tsx'
# → réécrire vers @/modules/intelligence (barrel du pilier) OU @/modules/intelligence/ia/hooks/useX

# 5. Ajouter les exports au barrel du pilier intelligence
# Ouvrir src/modules/intelligence/index.ts et ajouter :
# export { useGeminiAgent } from './ia/hooks/useGeminiAgent';
# export { useNexusFleet } from './ia/hooks/useNexusFleet';
# export { useStrategicOracle } from './ia/hooks/useStrategicOracle';
# export { useIntelligence } from './ia/hooks/useIntelligence';
# export { useCoreOracle } from './ia/hooks/useCoreOracle';

# 6. Vérifier qu'aucun kernel/lib/store n'importe depuis modules/intelligence
grep -rn "from '@/modules/intelligence" src/kernel src/lib src/store --include='*.ts*' | grep -v '\.test\.'

# 7. Gate complet
npx tsc --noEmit 2>&1 | grep -c "error TS"  # == 0
npx madge --circular --extensions ts,tsx src 2>/dev/null | grep -cE '^[0-9]+\)'  # <= 2
./scripts/agent-gate.sh

# 8. Commit (background)
git add -A
git commit -m "refactor(rapatriement): étape 8c — hooks intelligence → modules/intelligence/ia/hooks

TSC=0, cycles=2, lib→modules=0"
```

**Pièges spécifiques** :
- `useStrategicOracle` importe `useNexusFleet` en relatif `./useNexusFleet` → survivra si les deux bougent ensemble ✅
- `useCoreOracle` importe `@/lib/adapters/MasterBridge` → direction correcte (modules→lib ✅)
- Vérifier que `useGeminiAgent` n'a pas de doublon dans `modules/intelligence/ia/agency/` ou `ia/fleet/`

---

### Sous-étape 8d — Hooks design → `design/hooks/`

**5 fichiers** : `useBrandEditor`, `useVerticalComponent`, `useFirestoreBrand`, `useBrandCapabilities`, `useLexicon`

**Pourquoi cette destination** : hooks de présentation (branding, vertical UI, lexique). `useBrandEditor` importe `@/infrastructure/services/storage` → interdit en kernel/ si `@/infrastructure` résout au-dessus de kernel/ (voir pré-check).

#### Actions précises

```bash
# 1. Créer le dossier cible
mkdir -p src/design/hooks

# 2. Déplacer les 5 fichiers
git mv src/shared/hooks/useBrandEditor.ts       src/design/hooks/useBrandEditor.ts
git mv src/shared/hooks/useVerticalComponent.ts src/design/hooks/useVerticalComponent.ts
git mv src/shared/hooks/useFirestoreBrand.ts    src/design/hooks/useFirestoreBrand.ts
git mv src/shared/hooks/useBrandCapabilities.ts src/design/hooks/useBrandCapabilities.ts
git mv src/shared/hooks/useLexicon.ts           src/design/hooks/useLexicon.ts

# 3. Re-TSC immédiatement
npx tsc --noEmit 2>&1 | grep "error TS" | head -20

# 4. Identifier et réécrire les importeurs directs
grep -rn "shared/hooks/useBrand\|shared/hooks/useVertical\|shared/hooks/useFirestore\|shared/hooks/useLexicon" src --include='*.ts' --include='*.tsx'
# → réécrire vers @design/hooks/useX ou @/design/hooks/useX

# 5. Créer un barrel design/hooks/index.ts
cat > src/design/hooks/index.ts << 'EOF'
export { useBrandEditor } from './useBrandEditor';
export { useVerticalComponent } from './useVerticalComponent';
export { useFirestoreBrand } from './useFirestoreBrand';
export { useBrandCapabilities } from './useBrandCapabilities';
export { useLexicon } from './useLexicon';
EOF

# 6. Gate
npx tsc --noEmit 2>&1 | grep -c "error TS"  # == 0
./scripts/agent-gate.sh

# 7. Commit (background)
git add -A
git commit -m "refactor(rapatriement): étape 8d — hooks design → design/hooks

TSC=0, cycles=2"
```

**Pièges spécifiques** :
- `useLexicon` importe `@/kernel/providers/NexusCoreProvider` (déjà migré en 8a) et `@/kernel/plugins/IVerticalLexicon` (déjà en kernel) → direction correcte (design→kernel ✅)
- `useVerticalComponent` importe `@/kernel/plugins/*` et `VerticalUIProvider` (en kernel/providers) → ✅
- `useBrandEditor` importe `@/infrastructure/services/storage` → si pré-check montre que `@/infrastructure` mappe dans modules/, changer la destination en `modules/commerce/catalog/hooks/` ou `modules/facility/assets/hooks/` selon le contenu

---

### Sous-étape 8e — Hooks divers (orchestration + nettoyage)

**3 fichiers** : `useDLQQuarantine`, `useConnector`, `useManagement`

#### 8e-1 — `useDLQQuarantine` → `orchestration/hooks/`

```bash
# Vérifier que le dossier orchestration/ existe
ls src/orchestration/ 2>/dev/null || echo "ABSENT — créer"
mkdir -p src/orchestration/hooks

git mv src/shared/hooks/useDLQQuarantine.ts src/orchestration/hooks/useDLQQuarantine.ts

# Réécrire les importeurs (0 importeur externe selon le plan — vérifier)
grep -rn "shared/hooks/useDLQQuarantine" src --include='*.ts' --include='*.tsx'

npx tsc --noEmit 2>&1 | grep "error TS"
```

#### 8e-2 — `useConnector` → `kernel/hooks/` (type-only, neutre)

```bash
# useConnector.ts ne contient que le type UseConnectorResult
# → doit rester dans une couche NEUTRE (kernel ou lib), jamais modules
# → kernel/hooks/ car les autres hooks nexus-core y sont déjà

git mv src/shared/hooks/useConnector.ts src/kernel/hooks/useConnector.ts

# Réécrire les importeurs
grep -rn "shared/hooks/useConnector\|shared/hooks.*UseConnectorResult" src --include='*.ts' --include='*.tsx'
# → réécrire vers @/kernel/hooks/useConnector

npx tsc --noEmit 2>&1 | grep "error TS"
```

#### 8e-3 — `useManagement` → vérifier puis supprimer ou garder

```bash
# 0 importeur externe → vérifier que c'est vraiment mort
grep -rn "useManagement" src --include='*.ts' --include='*.tsx' | grep -v "shared/hooks/useManagement"
# Si 0 résultat → supprimer
git rm src/shared/hooks/useManagement.ts

# Si des importeurs → git mv vers kernel/hooks/useManagement.ts
```

#### Gate + commit

```bash
npx tsc --noEmit 2>&1 | grep -c "error TS"  # == 0
./scripts/agent-gate.sh

git add -A
git commit -m "refactor(rapatriement): étape 8e — hooks divers → orchestration + kernel + suppression

TSC=0, cycles=2"
```

---

### Sous-étape 8g — Élimination du barrel `@/shared/hooks` (DERNIER)

**C'est l'étape finale et la plus impactante** : 66 importeurs du barrel.

**Principe** : créer `kernel/hooks/index.ts` comme barrel unifié qui re-exporte tout, puis réécrire les 66 `@/shared/hooks'` → `@/kernel/hooks'`, puis supprimer `shared/hooks/index.ts`.

#### Actions précises

```bash
# 1. Inspecter l'état actuel du barrel shared/hooks/index.ts
cat src/shared/hooks/index.ts
# → doit ne plus re-exporter que des chemins déjà migrés (lib/hooks, kernel/hooks, kernel/providers)

# 2. Créer kernel/hooks/index.ts — barrel unifié
# Il doit re-exporter :
# - tous les hooks lib/hooks (via @/lib/hooks)
# - tous les hooks kernel/hooks/*
# - les hooks de NexusCoreContext (useNexusCore, useAuth via @/kernel/providers/NexusCoreContext)
# - le type UseConnectorResult (via @/kernel/hooks/useConnector)

cat > src/kernel/hooks/index.ts << 'EOF'
// Barrel unifié — remplace @/shared/hooks
// Hooks utilitaires génériques (lib)
export * from '@/lib/hooks';

// Hooks nexus-core (kernel)
export { useTenant } from './useTenant';
export { useSettings } from './useSettings';
export { useNexusStatus } from './useNexusStatus';
export { useNexusMutation } from './useNexusMutation';
export { useActionPermission } from './useActionPermission';
export { useTabAccess } from './useTabAccess';
export { usePageAccess } from './usePageAccess';
export { useSovereignSwitchboard } from './useSovereignSwitchboard';
export { useDataMigration } from './useDataMigration';
export { useTenantLifecycle } from './useTenantLifecycle';
export { useLanguage } from './useLanguage';
export { useNotifications } from './useNotifications';
export { useUI } from './useUI';
export type { UseConnectorResult } from './useConnector';

// Hooks NexusCoreContext (kernel/providers)
export { useNexusCore, useAuth } from '@/kernel/providers/NexusCoreContext';
EOF

# 3. Réécrire les 66 importeurs du barrel
# ⚠️ Quote fermante ' après hooks — pour ne PAS toucher @/shared/hooks/useX (imports directs)
grep -rl "@/shared/hooks'" src --include='*.ts' --include='*.tsx' | xargs sed -i '' "s|@/shared/hooks'|@/kernel/hooks'|g"
grep -rl "@shared/hooks'" src --include='*.ts' --include='*.tsx' | xargs sed -i '' "s|@shared/hooks'|@kernel/hooks'|g"

# Variantes avec double-quote
grep -rl '@/shared/hooks"' src --include='*.ts' --include='*.tsx' | xargs sed -i '' 's|@/shared/hooks"|@/kernel/hooks"|g'
grep -rl '@shared/hooks"' src --include='*.ts' --include='*.tsx' | xargs sed -i '' 's|@shared/hooks"|@kernel/hooks"|g'

# 4. Vérifier qu'il ne reste plus d'importeur du barrel
grep -rn "@/\?shared/hooks'" src --include='*.ts' --include='*.tsx' | wc -l
# Attendu : 0

# 5. Supprimer shared/hooks/index.ts et les fichiers restants de shared/hooks/
# (tous les hooks individuels ont été déplacés en 8c, 8d, 8e)
ls src/shared/hooks/   # ne doit plus contenir que index.ts
git rm src/shared/hooks/index.ts

# 6. Vérifier que shared/ ne contient que schemas/
ls src/shared/
# Attendu : schemas/ seulement

# 7. Gate final COMPLET
npx tsc --noEmit 2>&1 | grep -c "error TS"           # == 0
npx madge --circular --extensions ts,tsx src 2>/dev/null | grep -cE '^[0-9]+\)'  # <= 2
grep -rn "from '@/modules/" src/kernel src/lib src/store --include='*.ts*' | grep -v '\.test\.' | wc -l  # == 0
./scripts/agent-gate.sh
sentrux check .   # gate architectural officiel (voit les cycles alias que madge rate)

# 8. Commit final (background)
git add -A
git commit -m "refactor(rapatriement): étape 8g — élimination barrel @/shared/hooks (66 importeurs → @/kernel/hooks)

shared/ réduit à schemas/ uniquement (3 fichiers gelés).
TSC=0, cycles=2, sentrux clean"
```

**Pièges spécifiques** :
- `kernel/hooks/index.ts` ne doit **jamais** re-exporter depuis `modules/` → violation `kernel→modules`
- Si `useNexusCore` / `useAuth` ne sont pas exportés par `kernel/providers/NexusCoreContext`, les ajouter d'abord à ce barrel
- Les imports directs `@/shared/hooks/useX` (sans quote fermante après `hooks`) ne sont PAS touchés par le sed — les vérifier séparément :
  ```bash
  grep -rn "@/shared/hooks/" src --include='*.ts' --include='*.tsx'
  # → réécrire manuellement vers la destination canonique de chaque hook
  ```

---

## 4. Vérification par sous-étape (template obligatoire)

```bash
# Avant chaque commit :
npx tsc --noEmit 2>&1 | grep -c "error TS"                                        # == 0
npx madge --circular --extensions ts,tsx src 2>/dev/null | grep -cE '^[0-9]+\)'   # <= 2
grep -rn "from '@/modules/" src/kernel src/lib src/store --include='*.ts*' | grep -v '\.test\.' | wc -l  # == 0
./scripts/agent-gate.sh

# Refs shared résiduelles du lot en cours :
grep -rn "@/\?shared/\(hooks\)" src --include='*.ts*' | wc -l
```

---

## 5. État cible final

```
src/shared/
└── schemas/         ← primitives.ts (GELÉ) · ui.ts · index.ts   [restent par design]
```

| Couche | Hooks ajoutés |
|---|---|
| `kernel/hooks/` | +1 (useConnector type) · total ~14 |
| `kernel/hooks/index.ts` | barrel unifié créé (remplace @/shared/hooks) |
| `design/hooks/` | 5 hooks branding/vertical (8d) |
| `modules/intelligence/ia/hooks/` | 5 hooks IA (8c) |
| `orchestration/hooks/` | 1 hook DLQ (8e) |

**Gate vert attendu** : TSC=0 · madge cycles=2 · kernel/lib/store→modules=0 · barrels=0 · sentrux clean.

---

## 6. Insights critiques (ne pas réapprendre)

1. **madge du gate = imports relatifs seulement** — ne détecte PAS les cycles alias. Lancer `sentrux check .` en fin d'étape 8g.
2. **`git mv` casse les imports relatifs internes** (`./x`, `../x`) — après chaque batch git mv → TSC immédiat, lire `TS2307`, corriger.
3. **Quote fermante dans le sed du barrel** : `"s|@/shared/hooks'|..."` (avec `'` après `hooks`) — sinon on touche les imports directs `@/shared/hooks/useX`.
4. **Imports directs** `@/shared/hooks/useX` ≠ barrel — les traiter séparément avec `grep -rn "@/shared/hooks/"`.
5. **Hook graphify** ≈ 2 min post-commit → tous les commits en `run_in_background: true`.
6. **kernel→modules = interdit** — `kernel/hooks/index.ts` ne doit re-exporter que des couches neutres (lib, kernel/).
7. **UIThemeProvider** est dans `kernel/providers/` (pas design) — vérifier après 8g si le barrel l'inclut correctement.

---

## 7. Rollback

Chaque sous-étape est un commit isolé. En cas de régression :
```bash
git revert <hash_du_commit_problématique>
# ou retour au HEAD propre avant 8c :
git reset --hard e1cc2657c
```
